

	for (i = 1 ; i < 21 ; i++) {
		int pt2 = vetor[i]
		int pt1 = vetor[i-1]
			if(pt2 > pt1){
				j = i-1;
				while vetor[j]>= pt2{
					j--;
				}
			copiavetor[0..i]
			switch(pt2, vetor[j]

# organizar 20 valores diferentes organizados sequencialmente na memória; 
# s1 recebe primeiro valor,s2 recebe segundo.

	addi a0, zero, 1        # a0 = i
	addi a1, zero, 21 	# fim loop
	addi s2, zero, 0        # pt2
	addi s1, zero, 0        # pt1
	addi s3, zero, 0        # j
	
	
	
	
	bne a0, a1, loopext
	
loopext:
	bge s2, s1, while  # vai para o while enquanto s2 > s1
	addi a0, a0, 1     # i++

while:
	addi a2, zero, 0  
	sub a2, a0, 1
	
	li s3, a2        # j = i -1
	
	
	
	

					
					